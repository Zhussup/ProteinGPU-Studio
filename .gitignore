# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/

# ML weights & artifacts (large)
data/models/
data/jobs/

# Built binaries
*.so
*.o
*.cu.o
hpc_core/build/

# Frontend
node_modules/
dist/

# Reports (regenerable, but keep figures)
# data/report/ — commit figures, ignore raw json? keep all for now

# OS
.DS_Store
__pycache__/
*.db
*.sqlite3
data/jobs/
backend/tests/_testdata_scan/
data/jobs.db*

# Demo recordings (the README GIF lives in docs/assets/, raw takes stay local)
data/demo/

# Local tooling
.claude/

# Private / scratch — never push
dum.md
/notes/
*.pptx

# docs: keep only assets (README GIF), everything else stays local
docs/*
!docs/assets/
